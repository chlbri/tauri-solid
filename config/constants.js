const CARGO_TOML = `${process.cwd()}/src-tauri/Cargo.toml`;
const NODE_JSON = `${process.cwd()}/package.json`;
const TAURI_JSON = `${process.cwd()}/src-tauri/tauri.conf.json`;

module.exports = { CARGO_TOML, TAURI_JSON, NODE_JSON };
